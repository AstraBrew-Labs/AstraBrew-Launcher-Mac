# 2026-09-10

## 资源管理 · 预设详情：拆分「依赖酒馆助手」指标
- 问题：预设详情第三张指标卡原为 `扩展格式`，值为 `依赖酒馆助手` / `标准`，语义错位（扩展格式 ≠ 依赖关系）。
- 改动 `src/pages/resource_manage.rs`：
  - 第三张指标卡改为独立语义的 `依赖酒馆助手`，值为 `是` / `否`。
  - 新增常量 `TAVERN_HELPER_ACCENT = Color::from_rgb8(142, 68, 220)`（沿用原紫色），为假时用 `INK_MUTED` 中性灰。
  - 新增导入 `astra_ui::INK_MUTED`。
- 改动 `src/lang/en.rs`：新增 `"是" => "Yes"`、`"否" => "No"`（`"依赖酒馆助手"` 已有映射）。
- `cargo check` 通过，0 error / 0 warning（仅第三方 `block v0.1.6` future-incompat 提示，与本改动无关）。
- 结论：`has_spreset`（检测 `extensions` 内含 spreset/tavern_helper 键）本质就是布尔依赖标记，格式与依赖 1:1 对应，因此不保留 `扩展格式` 卡。
